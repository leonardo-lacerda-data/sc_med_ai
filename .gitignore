# Credenciais — NUNCA versionar
.env
.streamlit/secrets.toml
*.pem
*.key

# Ambiente
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/

# Ferramentas de sessão e assistentes
.agents/
.claude/
.vs/

# Logs e sistema
*.log
.DS_Store
Thumbs.db

# --------------------------------------------------------------------
# chroma_db/ É VERSIONADO DE PROPÓSITO.
#
# São 2,1 MB. Mantê-lo no repositório permite que qualquer pessoa clone e
# rode sem precisar reindexar, e é requisito para publicar no Streamlit
# Community Cloud, que não tem etapa de build.
#
# A regra antiga era `data/index/`, caminho que o projeto não usa mais.
# --------------------------------------------------------------------
